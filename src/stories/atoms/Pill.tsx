const Pill = ({ name }: { name: string }) => (
    <div style={{ backgroundColor: 'lightgrey', maxWidth: 'fit-content', padding: '8px 24px', borderRadius: '1.875rem', textTransform: 'capitalize'}}>{name}</div>
)

export default Pill;
